# Exercise 7.1

Create `useField` hook and use it in the anecdote creation form

# Exercise 7.2

Add a button that clears all input fields. Add reset function to the `useField` hook

# Exercise 7.3

Fix spreading warning after added reset function

# Exercise 7.4

Add `useAnecdotes` hook. It should fetch all anecdotes from the server
